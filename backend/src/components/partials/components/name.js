import React from 'react'
import img from '../../../assets/images/name.png'
import {Image} from 'react-bootstrap'

const Name = (props ) => {
    
    return (
        <>
          <Image className='name' src={img} />
         </>
        )
    }
    
    export default Name
